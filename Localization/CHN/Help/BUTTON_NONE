应用程序检测级别：无
为与当前配置文件关联的应用程序禁用检测。在此模式下，%PRODUCTNAME% 不会收集运行时统计信息（例如帧率），也不会向关联的应用程序提供附加服务（例如屏幕显示）。

提示：
- 某些受保护的应用程序可能会将正确的应用程序检测、统计收集和屏幕显示渲染所需的可执行代码挂钩视为潜在威胁并拒绝运行。要解决此类问题，请尝试为该应用程序添加配置文件并降低应用程序检测级别。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
