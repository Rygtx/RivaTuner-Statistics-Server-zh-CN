帧时间计算点
典型的游戏渲染循环由以下阶段组成：

[A_1]CPU 渲染[B_1]帧呈现[C_1]
[A_2]CPU 渲染[B_2]帧呈现[C_2]
...
[A_n]CPU 渲染[B_n]帧呈现[C_n]


A_n 点定义每个新帧开始的时刻，通常在此阶段对输入进行采样，CPU 开始渲染表示此时间戳模拟的 3D 世界的新帧。
B_n 点定义 CPU 完成渲染新帧并尝试通过 3D API 帧呈现调用呈现它的时刻。3D API 捕获 3D 命令缓冲区并将其放入呈现队列，GPU 从那时起开始异步处理它。
C_n 点定义 3D API 完成处理帧呈现调用并将控制权返回给游戏渲染线程的时刻。C_n 点实际上与 An+1 点匹配，即渲染循环同时再次开始渲染新帧。
游戏内帧率和帧时间计数器通常将帧时间定义为两个连续帧的 A_n 和 A_n-1 时间点之差。%PRODUCTNAME% 默认使用相同的时间点来计算帧时间。
然而，一些基准测试应用程序不在游戏挂钩级别工作，无法访问此类应用程序特定的时间。它们只能依赖 DXGI 帧呈现统计信息，因此它们对帧时间的定义略有不同，将其计算为 B_n 和 B_n-1 时间点之差。
以此方式测量的帧时间由于其不同性质以及在不同时刻采样时间戳，将与游戏内性能计数器或 %PRODUCTNAME% 测量的帧时间不匹配。
如有必要，您可以使用此选项切换到此类帧时间计算模型。

提示：
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
