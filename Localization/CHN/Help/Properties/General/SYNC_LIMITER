帧率限制模式
允许选择以下帧率限制模式之一，它们具有不同的优先级并服务于不同的目的：

- 异步 - 优先考虑严格的最小帧时间（和最高帧率）限制，高于其他一切。在此模式下，限制器绝不允许挂钩的 3D 应用程序开始渲染新帧，直到自开始渲染上一帧以来的时间超过限制。在此模式下，当帧率高于限制时，帧开始时序同步到高精度系统时钟，但当帧率低于限制时，帧开始时序是异步的并允许漂移。在此模式下，在帧呈现点计算的帧时间是抖动的，但在帧开始点计算的帧时间是平稳的。此模式面向最小输入延迟。
- 前沿同步 - 优先考虑同步而非严格限制，将帧呈现调用的前沿同步到高精度系统时钟。换言之，它同步帧呈现时序。在此模式下，限制器允许某些帧超过限制，以便在卡顿后将下一帧呈现同步到时钟。在此模式下，在帧呈现点计算的帧时间是平稳的，但在帧开始点计算的帧时间是抖动的。此模式不面向输入延迟，并且由于在帧开始和帧呈现点之间人为等待，延迟可能会增加。
- 后沿同步 - 优先考虑同步而非严格限制，将帧呈现调用的后沿同步到高精度系统时钟。换言之，它同步帧开始时序。在此模式下，限制器允许某些帧超过限制，以便在卡顿后将下一帧开始同步到时钟。在此模式下，在帧呈现点计算的帧时间是抖动的，但在帧开始点计算的帧时间是平稳的。此模式面向最小输入延迟，其行为非常接近异步模式。
- NVIDIA Reflex - 在此模式下，%PRODUCTNAME% 禁用自身精确的帧节奏实现，并改用 NVIDIA 自己的帧率限制器。此模式旨在与 DLSS 帧生成结合使用，后者通常与任何第三方帧率限制器不兼容。您也可以在没有原生 NVIDIA Reflex 支持的游戏中使用此模式，在此类游戏中启用 NVIDIA Reflex 帧率限制器作为副作用也会启用 Reflex 低延迟模式。此模式仅在 NVIDIA GPU 和 Direct3D11/Direct3D12 应用程序中受支持。如果您尝试在不支持的硬件或不支持的应用程序中启用它，%PRODUCTNAME% 将回退到默认的异步帧率限制模式。

提示：
- 此设置特定于当前选定的应用程序配置文件。
- 您可以结合前沿/后沿同步帧率限制模式启用扫描线同步，以激活混合扫描线同步模式。在这种情况下，实际目标扫描线同步仅在帧率限制开始时执行一次，然后您可以使用系统时钟调整撕裂线位置。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
