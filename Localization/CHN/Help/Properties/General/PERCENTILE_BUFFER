百分位缓冲区
影响基于百分位的 1% 最低和 0.1% 最低指标的计算。这两个指标都基于排序的帧时间数组，反映从位于最慢帧的 1% 或 0.1% 边界处的单个帧时间计算得出的瞬时帧率。
此选项定义用于排序的帧时间缓冲区大小。缓冲区可以是无限制的，因此基准测试会话期间渲染的每一帧都会影响结果；也可以是滚动环形缓冲区，因此过旧的帧会被丢弃，仅处理基准测试的最后几秒。
如果您通过热键手动启动基准测试会话，建议使用无限制模式。如果您永久保持基准测试模式启用，并希望查看反映最后几秒游戏过程的 1% 和 0.1% 最低指标，则建议使用环形模式。

提示：
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
