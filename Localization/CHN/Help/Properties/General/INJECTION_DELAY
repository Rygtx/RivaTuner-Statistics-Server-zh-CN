注入延迟
调整延迟注入引擎的延迟（以毫秒为单位）。延迟注入旨在绕过某些其他屏幕显示应用程序（例如 64 位 Steam 叠加）中实现的屏幕显示独占性检查，并防止此类应用程序阻止其屏幕显示功能。延迟注入引擎可由用户配置，因此高级用户可以扩展触发延迟注入机制的 DLL 模块列表并调整延迟。默认情况下，延迟注入由 64 位 Steam 叠加和 Origin 游戏内叠加库触发，如果这些库加载到应用程序地址空间中，注入延迟设置为 15 秒。

提示：
- 此设置特定于当前选定的应用程序配置文件。
- 您可以将延迟设置为 0 以禁用延迟注入。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
