扫描线同步模式
允许在单倍、双倍和半倍扫描线同步模式之间切换。

在单倍扫描线同步模式下，帧率会在每个显示刷新周期内与光栅化器位置同步一次。换言之，您将帧率限制为刷新率，并实现垂直同步开启的效果，且撕裂线位于固定位置。可以将撕裂线向下移动并隐藏在垂直消隐区间内。
在双倍扫描线同步模式下，帧率会在每个显示刷新周期内与光栅化器位置同步两次。换言之，您将帧率限制为双倍刷新率，并实现垂直同步开启的效果，且两条撕裂线位于固定位置。两条撕裂线都可以向下移动，其中一条可以隐藏在垂直消隐区间内。
在半倍扫描线同步模式下，帧率会每两个显示刷新周期与光栅化器位置同步一次。换言之，您将帧率限制为刷新率的一半，并实现垂直同步开启的效果，且撕裂线位于固定位置。在这种情况下，可以将撕裂线向下移动并隐藏在垂直消隐区间内。

提示：
- 必须禁用垂直同步才能使扫描线同步功能正常工作。
- 您可以将扫描线同步与前/后边缘同步帧率限制模式结合使用，以激活混合扫描线同步模式。在这种情况下，实际的目标扫描线同步仅在帧率限制开始时执行一次，然后您可以使用系统时钟来调整撕裂线位置。
- 您可以使用移动条模式下的 FCAT 叠加层作为可视的撕裂线位置指示器。
- 您可以指定负的扫描线索引以设置相对于扫描线总数的偏移量。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
