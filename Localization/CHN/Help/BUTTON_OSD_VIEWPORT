屏幕显示坐标空间：视口
默认情况下，屏幕显示坐标空间与渲染视口的坐标空间相匹配，即应用程序最后渲染到的矩形区域。但是，某些应用程序（例如《星际争霸 II：自由之翼》）可以在帧渲染期间使用多个视口（例如为 3D 世界和 HUD 使用不同的视口等）。在这种情况下，屏幕显示原点会跟随在帧渲染期间定义的最后一个视口的某个角，而该位置可能位于不希望的位置。在此类应用程序中，可以使用屏幕显示坐标空间覆盖相关选项来恢复预期的原点位置。
此按钮选择渲染视口作为屏幕显示的坐标空间。

提示：
- 某些游戏的反作弊系统可能会将应用程序检测和屏幕显示渲染所需的游戏代码挂钩视为潜在的作弊行为，并阻止连接到多人游戏服务器。要解决此类问题，请尝试为该游戏添加配置文件并禁用屏幕显示支持，如果仍然无效，请尝试降低应用程序检测级别。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
